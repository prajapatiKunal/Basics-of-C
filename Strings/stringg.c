#include<stdio.h>
#include<string.h>

int main(){
    // char arr[] = {'H','e','l','l','o','_','B','a','b','y'};
    char str[] = "kunal"; 

    // gets(str);
    printf("Input is: %s\n", str);
    puts("the input is: ");
    puts(str);

    // printf("Size of string is: %d ", sizeof(str));


    // printf("%d", sizeof(arr[]));
    // int i = 0;
    // while (brr[i]!= '\0'){
    //     printf("%c", brr[i]);
    //     i++;
    // }

    return 0;
}